nav.navbar.navbar-default.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Youtube playlist Demo
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        - if (req.isAuthenticated())
          li(class=isActive('/playlists/new') ? 'active' : '')
            a(href="/playlists/new", title="new playlists") Add Playlist
          li
            a(href="/users/"+req.user.id, title="Profile") Profile
          li
            a(href="/logout", title="logout") Logout
        - else
          li(class=isActive('/login') ? 'active' : '')
            a(href="/login", title="Login") Login

      ul.nav.navbar-nav.navbar-right
        li
          a(href='http://www.snakeyue.com/') Home